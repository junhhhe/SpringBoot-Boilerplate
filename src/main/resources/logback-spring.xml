<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 로그 설정 프로퍼티 -->
    <property name="LOG_PATH" value="./logs"/>
    <springProperty name="LOG_FILE_NAME" source="log.config.filename" defaultValue="app"/>
    <springProperty name="LOG_MAX_FILE_SIZE" source="log.config.maxFileSize" defaultValue="10MB"/>
    <springProperty name="LOG_MAX_HISTORY" source="log.config.maxHistory" defaultValue="30"/>
    <springProperty name="LOG_TOTAL_SIZE_CAP" source="log.config.totalSizeCap" defaultValue="1GB"/>

    <!-- 로그 패턴에 색상 적용 -->
    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>

    <!-- 로그 패턴 설정 -->
    <property name="CONSOLE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-3relative]  %clr(%-5level) %clr(${PID:-}){magenta} %clr(---){faint} %clr([%15.15thread]){faint} %clr(%-40.40logger{36}){cyan} %clr(:){faint} %msg%n"/>
    <property name="FILE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-3relative] %-5level ${PID:-} --- [%15.15thread] %-40.40logger{36} : %msg%n"/>

    <!-- 콘솔 출력 설정 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- dev -->
    <springProfile name="dev">
        <root level="info">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- stag -->
    <springProfile name="stag">
        <!-- 파일 출력 설정 -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
            <encoder>
                <pattern>${FILE_LOG_PATTERN}</pattern>
            </encoder>

            <!-- 에러 발생시 무시하도록 설정 -->
            <prudent>true</prudent>

            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- 롤링된 파일 명명 규칙 -->
                <fileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}/${LOG_FILE_NAME}.%i.log</fileNamePattern>
                <!-- 파일당 최대 크기 -->
                <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
                <!-- 보관 주기 -->
                <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
                <!-- 총 파일 크기 제한 -->
                <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
            </rollingPolicy>
        </appender>

        <root level="info">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        
        <logger name="org.springframework.web" level="info" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>

    <!-- prod -->
    <springProfile name="prod">
        <!-- 파일 출력 설정 -->
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/${LOG_FILE_NAME}.log</file>
            <encoder>
                <pattern>${FILE_LOG_PATTERN}</pattern>
            </encoder>

            <!-- 에러 발생시 무시하도록 설정 -->
            <prudent>true</prudent>

            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- 롤링된 파일 명명 규칙 -->
                <fileNamePattern>${LOG_PATH}/%d{yyyy-MM-dd}/${LOG_FILE_NAME}.%i.log</fileNamePattern>
                <!-- 파일당 최대 크기 -->
                <maxFileSize>${LOG_MAX_FILE_SIZE}</maxFileSize>
                <!-- 보관 주기 -->
                <maxHistory>${LOG_MAX_HISTORY}</maxHistory>
                <!-- 총 파일 크기 제한 -->
                <totalSizeCap>${LOG_TOTAL_SIZE_CAP}</totalSizeCap>
            </rollingPolicy>
        </appender>

        <root level="info">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
        
        <logger name="org.springframework.web" level="info" additivity="false">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>
</configuration>
 